/** Stable hook for desktop layout rules targeting the page screen root */
export const SCREEN_ROOT_CLASS = "lf-screen";

/** Stable hook for the phone column root (cart overlay, desktop layout) */
export const APP_PHONE_ID = "app-phone";

/** Stable hook for desktop layout rules targeting the main app column */
export const APP_MAIN_CLASS = "lf-app-main";

/** Stable hook for hiding mobile header on desktop */
export const APP_HEADER_CLASS = "lf-app-header";

/** Stable hook for desktop top navigation bar */
export const SITE_TOPBAR_CLASS = "lf-site-topbar";
